const translation = require('./translation.json');
const routes = require('./routes.json');
const home = require('./home.json');
const pagina = require('./pagina.json');

const en = {
  translation,
  routes,
  home,
  pagina,
};

export default en;
